# Dashboard 仪表盘重构 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构 Dashboard 从卡片网格升级为三段式仪表盘（统计卡片 + 分组列表 + 最近活动），移除 Onboarding 阻塞页面。

**Architecture:** 新增 8 个前端组件（StatCards, StatCard, PairListSection, PairListGroup, RecentActivityPanel, ActivityItem, QuickActionsFAB, EmptyStateGuide），1 个 Rust 命令（get_recent_activities）。App.tsx 修改路由逻辑，Dashboard.tsx 重构布局。

**Tech Stack:** React 19, TypeScript, Tailwind CSS v4, Framer Motion, Zustand, Node.js test runner, Tauri 2.x

**Spec:** `docs/superpowers/specs/2026-05-02-v1.5.0-dashboard-redesign-design.md`

---

## Chunk 1: StatCards + 空状态引导

**Scope:** 统计卡片组件 + 空状态引导组件 + 基础测试

### File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/renderer/src/components/StatCards.tsx` | 创建 | 统计卡片容器，响应式网格 |
| `src/renderer/src/components/StatCard.tsx` | 创建 | 单个统计卡片 |
| `src/renderer/src/components/EmptyStateGuide.tsx` | 创建 | 空状态引导内容 |
| `src/renderer/src/components/Dashboard.tsx` | 修改 | 集成 StatCards 和 EmptyStateGuide |
| `tests/statCards.test.ts` | 创建 | StatCards 组件测试 |
| `tests/emptyStateGuide.test.ts` | 创建 | EmptyStateGuide 组件测试 |
| `src/renderer/src/locales/en/translation.json` | 修改 | 新增 i18n key |
| `src/renderer/src/locales/zh/translation.json` | 修改 | 新增 i18n key |

---

### Task 1: 创建 StatCard 组件

- [ ] **Step 1: Write the failing test**

Create `tests/statCard.test.ts`:

```ts
import assert from 'node:assert/strict'
import test from 'node:test'
import { readFile } from 'node:fs/promises'

const source = await readFile(
  new URL('../src/renderer/src/components/StatCard.tsx', import.meta.url),
  'utf8'
)

test('StatCard exports StatCard component', () => {
  assert.match(source, /export (function StatCard|const StatCard)/)
})

test('StatCard accepts value, label, and color props', () => {
  assert.match(source, /value/)
  assert.match(source, /label/)
  assert.match(source, /color/)
})

test('StatCard uses cn for class merging', () => {
  assert.match(source, /from ['"]\.\.\/.*utils['"]/)
  assert.match(source, /\bcn\b/)
})

test('StatCard uses glass-card base styling', () => {
  assert.match(source, /glass-card/)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
node --import tsx --test tests/statCard.test.ts
```

Expected: FAIL with "ENOENT: no such file or directory"

- [ ] **Step 3: Create StatCard component**

Create `src/renderer/src/components/StatCard.tsx`:

```tsx
import React from 'react'
import { cn } from '../lib/utils'

interface StatCardProps {
  value: number | string
  label: string
  color: 'primary' | 'green' | 'amber' | 'gray'
  className?: string
  'data-testid'?: string
}

const colorMap: Record<string, string> = {
  primary: 'from-primary/10 to-primary/5 border-primary/20 text-primary',
  green: 'from-green-500/10 to-green-500/5 border-green-500/20 text-green-500',
  amber: 'from-amber-500/10 to-amber-500/5 border-amber-500/20 text-amber-500',
  gray: 'from-gray-500/10 to-gray-500/5 border-gray-500/20 text-gray-500',
}

export function StatCard({
  value,
  label,
  color,
  className,
  'data-testid': testId,
}: StatCardProps): React.ReactNode {
  return (
    <div
      className={cn(
        'glass-card flex flex-col items-center gap-1 rounded-xl border bg-gradient-to-br p-4',
        colorMap[color],
        className
      )}
      data-testid={testId}
    >
      <div className="text-2xl font-semibold tracking-tight">{value}</div>
      <div className="text-xs font-medium uppercase tracking-wider opacity-70">{label}</div>
    </div>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
node --import tsx --test tests/statCard.test.ts
```

Expected: All tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/components/StatCard.tsx tests/statCard.test.ts
git commit -m "feat: add StatCard component with color variants"
```

---

### Task 2: 创建 StatCards 容器组件

- [ ] **Step 1: Write the failing test**

Create `tests/statCards.test.ts`:

```ts
import assert from 'node:assert/strict'
import test from 'node:test'
import { readFile } from 'node:fs/promises'

const source = await readFile(
  new URL('../src/renderer/src/components/StatCards.tsx', import.meta.url),
  'utf8'
)

test('StatCards exports StatCards component', () => {
  assert.match(source, /export (function StatCards|const StatCards)/)
})

test('StatCards accepts stats object with total/running/paused/finished', () => {
  assert.match(source, /total/)
  assert.match(source, /running/)
  assert.match(source, /paused/)
  assert.match(source, /finished/)
})

test('StatCards renders 4 StatCard children', () => {
  assert.match(source, /<StatCard/g)
})

test('StatCards uses responsive grid layout', () => {
  assert.match(source, /grid/)
  assert.match(source, /md:grid-cols-4/)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
node --import tsx --test tests/statCards.test.ts
```

Expected: FAIL

- [ ] **Step 3: Create StatCards component**

Create `src/renderer/src/components/StatCards.tsx`:

```tsx
import React from 'react'
import { useTranslation } from 'react-i18next'
import { StatCard } from './StatCard'

interface StatCardsProps {
  total: number
  running: number
  paused: number
  finished: number
  className?: string
}

export function StatCards({
  total,
  running,
  paused,
  finished,
  className,
}: StatCardsProps): React.ReactNode {
  const { t } = useTranslation()

  return (
    <div className={cn('grid grid-cols-2 gap-3 md:grid-cols-4', className)}>
      <StatCard value={total} label={t('dashboard.stats.total')} color="primary" />
      <StatCard value={running} label={t('dashboard.stats.running')} color="green" />
      <StatCard value={paused} label={t('dashboard.stats.paused')} color="amber" />
      <StatCard value={finished} label={t('dashboard.stats.finished')} color="gray" />
    </div>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
node --import tsx --test tests/statCards.test.ts
```

Expected: All tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/components/StatCards.tsx tests/statCards.test.ts
git commit -m "feat: add StatCards container with responsive grid"
```

---

### Task 3: 创建 EmptyStateGuide 组件

- [ ] **Step 1: Write the failing test**

Create `tests/emptyStateGuide.test.ts`:

```ts
import assert from 'node:assert/strict'
import test from 'node:test'
import { readFile } from 'node:fs/promises'

const source = await readFile(
  new URL('../src/renderer/src/components/EmptyStateGuide.tsx', import.meta.url),
  'utf8'
)

test('EmptyStateGuide exports EmptyStateGuide component', () => {
  assert.match(source, /export (function EmptyStateGuide|const EmptyStateGuide)/)
})

test('EmptyStateGuide accepts onCreatePair callback prop', () => {
  assert.match(source, /onCreatePair/)
})

test('EmptyStateGuide includes step-by-step guide', () => {
  assert.match(source, /[Ss]tep|[Gg]uide|[Ii]mport/)
})

test('EmptyStateGuide uses GlassButton for primary action', () => {
  assert.match(source, /GlassButton/)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
node --import tsx --test tests/emptyStateGuide.test.ts
```

Expected: FAIL

- [ ] **Step 3: Create EmptyStateGuide component**

Create `src/renderer/src/components/EmptyStateGuide.tsx`:

```tsx
import React from 'react'
import { useTranslation } from 'react-i18next'
import { Plus, FolderOpen, Brain, Zap, RefreshCw } from 'lucide-react'
import { GlassButton } from './ui/GlassButton'
import { GlassCard } from './ui/GlassCard'

interface EmptyStateGuideProps {
  onCreatePair: () => void
  onImportConfig?: () => void
}

export function EmptyStateGuide({
  onCreatePair,
  onImportConfig,
}: EmptyStateGuideProps): React.ReactNode {
  const { t } = useTranslation()

  const steps = [
    { icon: <FolderOpen size={16} />, text: t('emptyState.step1') },
    { icon: <Brain size={16} />, text: t('emptyState.step2') },
    { icon: <Zap size={16} />, text: t('emptyState.step3') },
    { icon: <RefreshCw size={16} />, text: t('emptyState.step4') },
  ]

  return (
    <div className="flex h-full flex-col items-center justify-center p-8">
      <div className="relative mb-6 flex flex-col items-center">
        <div className="glass-card relative flex h-16 w-16 items-center justify-center rounded-2xl border border-border/50 bg-background/80 shadow-xl">
          <Brain size={28} className="text-blue-500" />
          <Zap size={24} className="absolute -right-1 -bottom-1 text-purple-500" />
        </div>
      </div>

      <h2 className="mb-2 text-xl font-semibold text-foreground">{t('emptyState.welcome')}</h2>
      <p className="mb-6 max-w-md text-center text-sm leading-relaxed text-muted-foreground">
        {t('emptyState.description')}
      </p>

      <div className="mb-8 w-full max-w-sm space-y-2">
        {steps.map((step, index) => (
          <GlassCard key={index} className="flex items-center gap-3 p-3">
            <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary">
              {step.icon}
            </div>
            <span className="text-sm text-foreground">{step.text}</span>
          </GlassCard>
        ))}
      </div>

      <div className="flex flex-col items-center gap-3">
        <GlassButton variant="primary" size="lg" onClick={onCreatePair} icon={<Plus size={16} />}>
          {t('emptyState.createFirst')}
        </GlassButton>
        {onImportConfig && (
          <button
            onClick={onImportConfig}
            className="text-sm text-muted-foreground underline-offset-4 hover:text-foreground hover:underline"
          >
            {t('emptyState.importConfig')}
          </button>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
node --import tsx --test tests/emptyStateGuide.test.ts
```

Expected: All tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/components/EmptyStateGuide.tsx tests/emptyStateGuide.test.ts
git commit -m "feat: add EmptyStateGuide with step-by-step onboarding"
```

---

### Task 4: 添加 i18n 翻译

- [ ] **Step 1: Add English translations**

Modify `src/renderer/src/locales/en/translation.json`:

Add under `dashboard`:
```json
"stats": {
  "total": "All Pairs",
  "running": "Running",
  "paused": "Paused",
  "finished": "Finished"
}
```

Modify under `emptyState`:
```json
"welcome": "Welcome to The Pair",
"step1": "Select a working directory",
"step2": "Choose Mentor and Executor models",
"step3": "Describe your task",
"step4": "Create your first Pair and start collaborating",
"importConfig": "Import existing configuration →"
```

- [ ] **Step 2: Add Chinese translations**

Modify `src/renderer/src/locales/zh/translation.json`:

Add under `dashboard`:
```json
"stats": {
  "total": "全部 Pairs",
  "running": "运行中",
  "paused": "已暂停",
  "finished": "已完成"
}
```

Modify under `emptyState`:
```json
"welcome": "欢迎使用 The Pair",
"step1": "选择工作目录",
"step2": "选择 Mentor 和 Executor 模型",
"step3": "描述你的任务",
"step4": "创建第一个 Pair，开始协作",
"importConfig": "导入现有配置 →"
```

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/locales/en/translation.json src/renderer/src/locales/zh/translation.json
git commit -m "feat: add i18n keys for dashboard stats and empty state"
```

---

### Task 5: 集成到 Dashboard（基础布局）

- [ ] **Step 1: Modify Dashboard.tsx (Chunk 1 version with placeholders)**

Modify `src/renderer/src/components/Dashboard.tsx`:

Replace the current return with the new three-section layout structure (Chunk 1 version):

```tsx
import React from 'react'
import { useTranslation } from 'react-i18next'
import { usePairStore, Pair } from '../store/usePairStore'
import { isPairActive } from '../lib/pairStatus'
import { StatCards } from './StatCards'
import { EmptyStateGuide } from './EmptyStateGuide'

export function Dashboard({
  onSelectPair,
  onDeletePair,
  deletingPairId,
  onCreatePair,
}: {
  onSelectPair: (p: Pair) => void
  onDeletePair: (pair: Pair) => void
  deletingPairId: string | null
  onCreatePair: () => void
}): React.ReactNode {
  const { t } = useTranslation()
  const pairs = usePairStore((state) => state.pairs)

  const stats = {
    total: pairs.length,
    running: pairs.filter((p) => isPairActive(p.status)).length,
    paused: pairs.filter((p) => p.status === 'Paused').length,
    finished: pairs.filter((p) => p.status === 'Finished').length,
  }

  return (
    <div className="relative h-full overflow-hidden">
      <div className="absolute inset-0 bg-gradient-to-br from-background via-muted/15 to-background" />
      <div className="absolute inset-0 bg-[radial-gradient(circle_at_top_left,rgba(59,130,246,0.14),transparent_32%),radial-gradient(circle_at_top_right,rgba(168,85,247,0.14),transparent_30%)]" />

      <div className="relative z-10 flex h-full flex-col p-6">
        {/* Stat Cards - always visible */}
        <StatCards
          total={stats.total}
          running={stats.running}
          paused={stats.paused}
          finished={stats.finished}
          className="mb-6"
        />

        {/* Empty state or placeholder for list */}
        {pairs.length === 0 ? (
          <div className="flex flex-1 items-center justify-center">
            <EmptyStateGuide onCreatePair={onCreatePair} />
          </div>
        ) : (
          <div className="flex flex-1 items-center justify-center text-muted-foreground">
            {t('dashboard.listComingSoon')}
          </div>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Add temporary i18n key**

Add to `en/translation.json` under `dashboard`:
```json
"listComingSoon": "Pair list coming soon..."
```

Add to `zh/translation.json` under `dashboard`:
```json
"listComingSoon": "Pair 列表即将推出..."
```

- [ ] **Step 3: Run typecheck**

```bash
npm run typecheck:web
```

Expected: No errors

- [ ] **Step 4: Run tests**

```bash
node --import tsx --test tests/statCard.test.ts tests/statCards.test.ts tests/emptyStateGuide.test.ts
```

Expected: All PASS

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/components/Dashboard.tsx src/renderer/src/locales/
git commit -m "refactor: integrate StatCards and EmptyStateGuide into Dashboard layout"
```

---

### Task 6: 修改 App.tsx 路由逻辑

- [ ] **Step 1: Remove OnboardingWizard blocking**

Modify `src/renderer/src/App.tsx`:

Remove these lines:
```tsx
const OnboardingWizard = lazy(() =>
  import('./components/OnboardingWizard').then(({ OnboardingWizard }) => ({
    default: OnboardingWizard
  }))
)
```

Remove `showOnboarding` variable:
```tsx
// REMOVE: const showOnboarding = pairsLoaded && pairs.length === 0
```

Replace the conditional rendering block:
```tsx
// BEFORE:
{showOnboarding ? (
  <Suspense fallback={<ViewFallback />}>
    <OnboardingWizard onComplete={() => {}} />
  </Suspense>
) : (
  <div className="flex h-full flex-col">...</div>
)}

// AFTER:
<div className="flex h-full flex-col">...</div>
```

Remove `!showOnboarding` wrapper from modals:
```tsx
// BEFORE: {!showOnboarding && (<Suspense>...</Suspense>)}
// AFTER: <Suspense>...</Suspense>
```

- [ ] **Step 2: Run typecheck**

```bash
npm run typecheck:web
```

Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/App.tsx
git commit -m "refactor: remove OnboardingWizard blocking page, always show Dashboard"
```

---

## Chunk 2: PairListSection + 分组列表

**Scope:** 按状态分组的 Pair 列表组件，hover 快捷操作

### File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/renderer/src/components/PairListSection.tsx` | 创建 | 分组列表容器 |
| `src/renderer/src/components/PairListGroup.tsx` | 创建 | 状态分组容器 |
| `tests/pairListSection.test.ts` | 创建 | 列表分组逻辑测试 |

---

### Task 7: 创建 PairListGroup 组件

- [ ] **Step 1: Write the failing test**

Create `tests/pairListSection.test.ts`:

```ts
import assert from 'node:assert/strict'
import test from 'node:test'
import { readFile } from 'node:fs/promises'

const pairListGroup = await readFile(
  new URL('../src/renderer/src/components/PairListGroup.tsx', import.meta.url),
  'utf8'
)

const pairListSection = await readFile(
  new URL('../src/renderer/src/components/PairListSection.tsx', import.meta.url),
  'utf8'
)

test('PairListGroup exports PairListGroup component', () => {
  assert.match(pairListGroup, /export (function PairListGroup|const PairListGroup)/)
})

test('PairListGroup accepts title and pairs props', () => {
  assert.match(pairListGroup, /title/)
  assert.match(pairListGroup, /[Pp]airs/)
})

test('PairListGroup renders pair items', () => {
  assert.match(pairListGroup, /onSelect|onClick/)
})

test('PairListSection exports PairListSection component', () => {
  assert.match(pairListSection, /export (function PairListSection|const PairListSection)/)
})

test('PairListSection groups pairs by status', () => {
  assert.match(pairListSection, /filter|group/i)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
node --import tsx --test tests/pairListSection.test.ts
```

Expected: FAIL

- [ ] **Step 3: Create PairListGroup component**

Create `src/renderer/src/components/PairListGroup.tsx`:

```tsx
import React from 'react'
import { motion } from 'framer-motion'
import { cn } from '../lib/utils'
import { type Pair } from '../store/usePairStore'
import { StatusBadge } from './StatusBadge'
import { fadeInUp } from '../lib/animations'

interface PairListGroupProps {
  title: string
  pairs: Pair[]
  onSelectPair: (pair: Pair) => void
  onPausePair: (pairId: string) => void
  onResumePair: (pairId: string) => void
  onDeletePair: (pair: Pair) => void
  deletingPairId: string | null
  className?: string
}

export function PairListGroup({
  title,
  pairs,
  onSelectPair,
  onPausePair,
  onResumePair,
  onDeletePair,
  deletingPairId,
  className,
}: PairListGroupProps): React.ReactNode {
  if (pairs.length === 0) return null

  return (
    <div className={cn('space-y-2', className)}>
      <h3 className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
        {title}
      </h3>
      <div className="space-y-1">
        {pairs.map((pair) => (
          <motion.div
            key={pair.id}
            variants={fadeInUp}
            initial="hidden"
            animate="visible"
            className={cn(
              'group flex items-center gap-3 rounded-lg border border-transparent px-3 py-2.5',
              'hover:border-border/50 hover:bg-muted/30 cursor-pointer transition-colors'
            )}
            onClick={() => onSelectPair(pair)}
          >
            {/* Status + Name */}
            <div className="flex min-w-0 flex-1 items-center gap-2">
              <StatusBadge
                status={pair.status}
                stalled={
                  (pair.turn === 'mentor' && pair.mentorActivity.phase === 'stalled') ||
                  (pair.turn === 'executor' && pair.executorActivity.phase === 'stalled')
                }
              />
              <div className="min-w-0">
                <div className="truncate text-sm font-medium text-foreground">{pair.name}</div>
                <div className="truncate text-[11px] font-mono text-muted-foreground" title={pair.directory}>
                  {pair.directory}
                </div>
              </div>
            </div>

            {/* Iteration info */}
            <div className="shrink-0 text-xs text-muted-foreground">
              {pair.iterations}/{pair.maxIterations}
            </div>

            {/* Hover actions */}
            <div className="hidden shrink-0 items-center gap-1 group-hover:flex">
              {pair.status === 'Paused' ? (
                <button
                  onClick={(e) => {
                    e.stopPropagation()
                    onResumePair(pair.id)
                  }}
                  className="rounded p-1 text-muted-foreground hover:bg-muted/50 hover:text-foreground"
                >
                  ▶
                </button>
              ) : isPairActive(pair.status) ? (
                <button
                  onClick={(e) => {
                    e.stopPropagation()
                    onPausePair(pair.id)
                  }}
                  className="rounded p-1 text-muted-foreground hover:bg-muted/50 hover:text-foreground"
                >
                  ⏸
                </button>
              ) : null}
              <button
                onClick={(e) => {
                  e.stopPropagation()
                  onDeletePair(pair)
                }}
                disabled={deletingPairId === pair.id}
                className="rounded p-1 text-muted-foreground hover:bg-destructive/10 hover:text-destructive"
              >
                ✕
              </button>
            </div>
          </motion.div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Create PairListSection component**

Create `src/renderer/src/components/PairListSection.tsx`:

```tsx
import React from 'react'
import { useTranslation } from 'react-i18next'
import { type Pair } from '../store/usePairStore'
import { isPairActive } from '../lib/pairStatus'
import { PairListGroup } from './PairListGroup'

interface PairListSectionProps {
  pairs: Pair[]
  onSelectPair: (pair: Pair) => void
  onPausePair: (pairId: string) => void
  onResumePair: (pairId: string) => void
  onDeletePair: (pair: Pair) => void
  deletingPairId: string | null
  onCreatePair: () => void
}

export function PairListSection({
  pairs,
  onSelectPair,
  onPausePair,
  onResumePair,
  onDeletePair,
  deletingPairId,
  onCreatePair,
}: PairListSectionProps): React.ReactNode {
  const { t } = useTranslation()

  const runningPairs = pairs.filter((p) => isPairActive(p.status))
  const pausedPairs = pairs.filter((p) => p.status === 'Paused')
  const idlePairs = pairs.filter((p) => p.status === 'Idle' || p.status === 'Finished')

  const hasActivePairs = runningPairs.length > 0 || pausedPairs.length > 0

  return (
    <div className="flex flex-col overflow-hidden">
      <div className="mb-4 flex items-center justify-between">
        <h2 className="text-sm font-semibold text-foreground">{t('dashboard.pairs.title')}</h2>
        <button
          onClick={onCreatePair}
          className="text-xs text-primary hover:text-primary/80"
        >
          + {t('dashboard.pairs.new')}
        </button>
      </div>

      <div className="flex-1 space-y-4 overflow-y-auto scrollbar-thin pr-2">
        {hasActivePairs && (
          <PairListGroup
            title={t('dashboard.groups.active')}
            pairs={runningPairs}
            onSelectPair={onSelectPair}
            onPausePair={onPausePair}
            onResumePair={onResumePair}
            onDeletePair={onDeletePair}
            deletingPairId={deletingPairId}
          />
        )}

        {pausedPairs.length > 0 && (
          <PairListGroup
            title={t('dashboard.groups.paused')}
            pairs={pausedPairs}
            onSelectPair={onSelectPair}
            onPausePair={onPausePair}
            onResumePair={onResumePair}
            onDeletePair={onDeletePair}
            deletingPairId={deletingPairId}
          />
        )}

        {idlePairs.length > 0 && (
          <PairListGroup
            title={t('dashboard.groups.completed')}
            pairs={idlePairs}
            onSelectPair={onSelectPair}
            onPausePair={onPausePair}
            onResumePair={onResumePair}
            onDeletePair={onDeletePair}
            deletingPairId={deletingPairId}
          />
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 5: Run test to verify it passes**

```bash
node --import tsx --test tests/pairListSection.test.ts
```

Expected: All PASS

- [ ] **Step 6: Add i18n keys**

Add to `en/translation.json`:
```json
"dashboard": {
  "pairs": {
    "title": "Pairs",
    "new": "New Pair"
  },
  "groups": {
    "active": "Active",
    "paused": "Paused",
    "completed": "Completed"
  }
}
```

Add to `zh/translation.json`:
```json
"dashboard": {
  "pairs": {
    "title": "Pairs",
    "new": "新建 Pair"
  },
  "groups": {
    "active": "运行中",
    "paused": "已暂停",
    "completed": "已完成"
  }
}
```

- [ ] **Step 7: Commit**

```bash
git add src/renderer/src/components/PairListSection.tsx src/renderer/src/components/PairListGroup.tsx tests/pairListSection.test.ts src/renderer/src/locales/
git commit -m "feat: add PairListSection with status grouping and hover actions"
```

---

### Task 8: 集成 PairListSection 到 Dashboard

- [ ] **Step 1: Replace Dashboard placeholder with PairListSection**

Modify `src/renderer/src/components/Dashboard.tsx`:

Replace the placeholder section (the `t('dashboard.listComingSoon')` div) with actual PairListSection:

1. Add import:
```tsx
import { PairListSection } from './PairListSection'
```

2. Add onPausePair and onResumePair props to the function signature:
```tsx
export function Dashboard({
  onSelectPair,
  onDeletePair,
  deletingPairId,
  onCreatePair,
  onPausePair,
  onResumePair,
}: {
  onSelectPair: (p: Pair) => void
  onDeletePair: (pair: Pair) => void
  deletingPairId: string | null
  onCreatePair: () => void
  onPausePair: (pairId: string) => void
  onResumePair: (pairId: string) => void
}): React.ReactNode {
```

3. Replace the placeholder div with PairListSection:
```tsx
// REPLACE THIS:
<div className="flex flex-1 items-center justify-center text-muted-foreground">
  {t('dashboard.listComingSoon')}
</div>

// WITH THIS:
<div className="flex flex-1 overflow-hidden">
  <PairListSection
    pairs={pairs}
    onSelectPair={onSelectPair}
    onPausePair={onPausePair}
    onResumePair={onResumePair}
    onDeletePair={onDeletePair}
    deletingPairId={deletingPairId}
    onCreatePair={onCreatePair}
  />
</div>
```

- [ ] **Step 2: Update App.tsx to pass pause/resume callbacks**

Modify `src/renderer/src/App.tsx`:

In the Dashboard render, add the pause/resume props:

```tsx
<Dashboard
  onSelectPair={(pair) => {
    setSelectedPairId(pair.id)
    if (pair.status === 'Idle' || pair.status === 'Finished') {
      setIsAssignTaskOpen(true)
    }
  }}
  onDeletePair={(pair) => {
    void handleDeletePair(pair)
  }}
  deletingPairId={deletingPairId}
  onCreatePair={() => setIsCreatePairOpen(true)}
  onPausePair={async (id: string) => {
    try { await pausePair(id) } catch (e) { console.error('[App] Failed to pause:', e) }
  }}
  onResumePair={async (id: string) => {
    try { await resumePair(id) } catch (e) { console.error('[App] Failed to resume:', e) }
  }}
/>
```

- [ ] **Step 3: Run typecheck**

```bash
npm run typecheck:web
```

Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add src/renderer/src/components/Dashboard.tsx src/renderer/src/App.tsx
git commit -m "refactor: integrate PairListSection into Dashboard with pause/resume"
```

---

## Chunk 3: QuickActionsFAB + 快捷键

**Scope:** 右下角浮动按钮 + 弹出菜单 + Cmd+K 快捷键

### File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/renderer/src/components/QuickActionsFAB.tsx` | 创建 | 浮动按钮 + 弹出菜单 |
| `src/renderer/src/components/App.tsx` | 修改 | 集成 FAB 到 Dashboard 视图 |
| `tests/quickActionsFAB.test.ts` | 创建 | FAB 组件测试 |

---

### Task 9: 创建 QuickActionsFAB 组件

- [ ] **Step 1: Write the failing test**

Create `tests/quickActionsFAB.test.ts`:

```ts
import assert from 'node:assert/strict'
import test from 'node:test'
import { readFile } from 'node:fs/promises'

const source = await readFile(
  new URL('../src/renderer/src/components/QuickActionsFAB.tsx', import.meta.url),
  'utf8'
)

test('QuickActionsFAB exports QuickActionsFAB component', () => {
  assert.match(source, /export (function QuickActionsFAB|const QuickActionsFAB)/)
})

test('QuickActionsFAB uses framer-motion for animations', () => {
  assert.match(source, /framer-motion/)
})

test('QuickActionsFAB has fixed positioning', () => {
  assert.match(source, /fixed|absolute/)
  assert.match(source, /bottom.*right|right.*bottom/i)
})

test('QuickActionsFAB menu items include create pair action', () => {
  assert.match(source, /[Cc]reate|[Nn]ew|[Pp]air/)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
node --import tsx --test tests/quickActionsFAB.test.ts
```

Expected: FAIL

- [ ] **Step 3: Create QuickActionsFAB component**

Create `src/renderer/src/components/QuickActionsFAB.tsx`:

```tsx
import React, { useState, useRef, useEffect } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { useTranslation } from 'react-i18next'
import { Zap, Plus, Settings, Book, Keyboard } from 'lucide-react'
import { cn } from '../lib/utils'

interface QuickActionItem {
  id: string
  label: string
  icon: React.ReactNode
  shortcut?: string
}

interface QuickActionsFABProps {
  onAction: (actionId: string) => void
  className?: string
}

const menuVariants = {
  hidden: { opacity: 0, y: 8, scale: 0.95 },
  visible: { opacity: 1, y: 0, scale: 1, transition: { duration: 0.15 } },
  exit: { opacity: 0, y: 8, scale: 0.95, transition: { duration: 0.1 } },
}

export function QuickActionsFAB({
  onAction,
  className,
}: QuickActionsFABProps): React.ReactNode {
  const { t } = useTranslation()
  const [isOpen, setIsOpen] = useState(false)
  const menuRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const handleClickOutside = (e: MouseEvent) => {
      if (menuRef.current && !menuRef.current.contains(e.target as Node)) {
        setIsOpen(false)
      }
    }
    document.addEventListener('mousedown', handleClickOutside)
    return () => document.removeEventListener('mousedown', handleClickOutside)
  }, [])

  const actions: QuickActionItem[] = [
    { id: 'create', label: t('quickActions.create'), icon: <Plus size={16} />, shortcut: '⌘N' },
    { id: 'settings', label: t('quickActions.settings'), icon: <Settings size={16} /> },
    { id: 'help', label: t('quickActions.help'), icon: <Book size={16} /> },
    { id: 'shortcuts', label: t('quickActions.shortcuts'), icon: <Keyboard size={16} />, shortcut: '⌘K' },
  ]

  return (
    <div ref={menuRef} className={cn('fixed bottom-6 right-6 z-50', className)}>
      <AnimatePresence>
        {isOpen && (
          <motion.div
            variants={menuVariants}
            initial="hidden"
            animate="visible"
            exit="exit"
            className="absolute bottom-14 right-0 w-56 rounded-xl border border-border/50 bg-background/95 p-1 shadow-xl backdrop-blur-xl"
          >
            {actions.map((action) => (
              <button
                key={action.id}
                onClick={() => {
                  onAction(action.id)
                  setIsOpen(false)
                }}
                className="flex w-full items-center gap-3 rounded-lg px-3 py-2 text-left text-sm text-foreground hover:bg-muted/50"
              >
                <span className="text-muted-foreground">{action.icon}</span>
                <span className="flex-1">{action.label}</span>
                {action.shortcut && (
                  <span className="text-[10px] font-mono text-muted-foreground">
                    {action.shortcut}
                  </span>
                )}
              </button>
            ))}
          </motion.div>
        )}
      </AnimatePresence>

      <button
        onClick={() => setIsOpen(!isOpen)}
        className={cn(
          'flex h-12 w-12 items-center justify-center rounded-full bg-primary text-primary-foreground shadow-lg',
          'hover:bg-primary/90 transition-colors',
          isOpen && 'rotate-45'
        )}
      >
        <Zap size={20} />
      </button>
    </div>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
node --import tsx --test tests/quickActionsFAB.test.ts
```

Expected: All PASS

- [ ] **Step 5: Add i18n keys**

Add to `en/translation.json`:
```json
"quickActions": {
  "create": "New Pair",
  "settings": "Settings",
  "help": "Help",
  "shortcuts": "Shortcuts"
}
```

Add to `zh/translation.json`:
```json
"quickActions": {
  "create": "新建 Pair",
  "settings": "设置",
  "help": "帮助",
  "shortcuts": "快捷键"
}
```

- [ ] **Step 6: Commit**

```bash
git add src/renderer/src/components/QuickActionsFAB.tsx tests/quickActionsFAB.test.ts src/renderer/src/locales/
git commit -m "feat: add QuickActionsFAB with animated popup menu"
```

---

### Task 10: 集成 FAB 到 App.tsx

- [ ] **Step 1: Add FAB to Dashboard view**

Modify `src/renderer/src/App.tsx`:

Add lazy import:
```tsx
const QuickActionsFAB = lazy(() =>
  import('./components/QuickActionsFAB').then(({ QuickActionsFAB }) => ({
    default: QuickActionsFAB
  }))
)
```

Add handler:
```tsx
const handleQuickAction = (actionId: string) => {
  switch (actionId) {
    case 'create':
      setIsCreatePairOpen(true)
      break
    case 'settings':
      setIsPairSettingsOpen(true)
      break
    // help and shortcuts - future implementation
  }
}
```

Add FAB in the Dashboard view (alongside the Dashboard component):
```tsx
<>
  <Dashboard ... />
  <Suspense fallback={null}>
    <QuickActionsFAB onAction={handleQuickAction} />
  </Suspense>
</>
```

- [ ] **Step 2: Run typecheck**

```bash
npm run typecheck:web
```

Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/App.tsx
git commit -m "feat: integrate QuickActionsFAB into App with action handlers"
```

---

## Chunk 4: RecentActivityPanel + Rust 命令

**Scope:** Rust 后端 `get_recent_activities` 命令 + 前端活动面板

### File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `src-tauri/src/recent_activity.rs` | 创建 | Rust 命令实现 |
| `src-tauri/src/types.rs` | 修改 | 新增 RecentActivity 类型 |
| `src-tauri/src/lib.rs` | 修改 | 注册命令 |
| `src/renderer/src/components/RecentActivityPanel.tsx` | 创建 | 活动面板 |
| `src/renderer/src/components/ActivityItem.tsx` | 创建 | 单条活动 |
| `src/renderer/src/components/Dashboard.tsx` | 修改 | 集成活动面板 |
| `tests/recentActivity.test.ts` | 创建 | Rust 命令前端测试 |

---

### Task 11: 创建 Rust RecentActivity 类型和命令

- [ ] **Step 1: Add RecentActivity type to types.rs**

Modify `src-tauri/src/types.rs`:

Add after existing types:
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum ActivityType {
    StatusChange,
    Handoff,
    Result,
    Acceptance,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RecentActivity {
    pub pair_id: String,
    pub pair_name: String,
    pub activity_type: ActivityType,
    pub description: String,
    pub timestamp: u64,
    pub role: Option<String>,
}
```

- [ ] **Step 2: Create recent_activity.rs module**

Create `src-tauri/src/recent_activity.rs`:

```rust
use crate::session_snapshot::SessionStore;
use crate::types::{ActivityType, RecentActivity};
use std::sync::Mutex;
use tauri::State;

#[tauri::command]
pub fn get_recent_activities(
    store: State<Mutex<SessionStore>>,
    limit: Option<usize>,
) -> Result<Vec<RecentActivity>, String> {
    let limit = limit.unwrap_or(10);
    let store = store.lock().map_err(|e| e.to_string())?;
    let snapshots = store.load_all().map_err(|e| e.to_string())?;

    let mut activities: Vec<RecentActivity> = Vec::new();

    for snapshot in snapshots {
        let pair_name = snapshot.name.clone();
        let pair_id = snapshot.pair_id.clone();

        // Extract status change events
        if let Some(status) = &snapshot.status {
            if matches!(status.as_str(), "Finished" | "Paused" | "Error") {
                activities.push(RecentActivity {
                    pair_id: pair_id.clone(),
                    pair_name: pair_name.clone(),
                    activity_type: ActivityType::StatusChange,
                    description: format!("Pair {}", status),
                    timestamp: snapshot
                        .current_run_finished_at
                        .unwrap_or(snapshot.created_at),
                    role: None,
                });
            }
        }

        // Extract meaningful messages
        for msg in &snapshot.messages {
            if matches!(
                msg.type.as_str(),
                "result" | "acceptance" | "handoff"
            ) {
                let description = msg
                    .content
                    .chars()
                    .take(80)
                    .collect::<String>();
                activities.push(RecentActivity {
                    pair_id: pair_id.clone(),
                    pair_name: pair_name.clone(),
                    activity_type: match msg.type.as_str() {
                        "result" => ActivityType::Result,
                        "acceptance" => ActivityType::Acceptance,
                        "handoff" => ActivityType::Handoff,
                        _ => ActivityType::StatusChange,
                    },
                    description,
                    timestamp: msg.timestamp,
                    role: Some(msg.from.clone()),
                });
            }
        }
    }

    // Sort by timestamp descending, take limit
    activities.sort_by(|a, b| b.timestamp.cmp(&a.timestamp));
    Ok(activities.into_iter().take(limit).collect())
}
```

- [ ] **Step 3: Register command in lib.rs**

Modify `src-tauri/src/lib.rs`:

Add module declaration:
```rust
mod recent_activity;
```

Add to invoke_handler:
```rust
recent_activity::get_recent_activities,
```

- [ ] **Step 4: Run Rust tests**

```bash
npm run test:rust
```

Expected: All existing tests PASS (new command has no unit tests yet)

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/recent_activity.rs src-tauri/src/types.rs src-tauri/src/lib.rs
git commit -m "feat: add get_recent_activities Tauri command"
```

---

### Task 12: 创建 ActivityItem 组件

- [ ] **Step 1: Write the failing test**

Add to `tests/recentActivity.test.ts`:

```ts
import assert from 'node:assert/strict'
import test from 'node:test'
import { readFile } from 'node:fs/promises'

const activityItem = await readFile(
  new URL('../src/renderer/src/components/ActivityItem.tsx', import.meta.url),
  'utf8'
)

test('ActivityItem exports ActivityItem component', () => {
  assert.match(activityItem, /export (function ActivityItem|const ActivityItem)/)
})

test('ActivityItem accepts activity prop with type/description/timestamp', () => {
  assert.match(activityItem, /activity/)
  assert.match(activityItem, /type|ActivityType/)
  assert.match(activityItem, /description/)
  assert.match(activityItem, /timestamp/)
})

test('ActivityItem uses line-clamp for text truncation', () => {
  assert.match(activityItem, /line-clamp/)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
node --import tsx --test tests/recentActivity.test.ts
```

Expected: FAIL

- [ ] **Step 3: Create ActivityItem component**

Create `src/renderer/src/components/ActivityItem.tsx`:

```tsx
import React from 'react'
import { cn } from '../lib/utils'
import {
  CheckCircle,
  PauseCircle,
  AlertCircle,
  ArrowRightLeft,
  FileText,
} from 'lucide-react'

export type ActivityType = 'status_change' | 'handoff' | 'result' | 'acceptance'

export interface Activity {
  pairId: string
  pairName: string
  type: ActivityType
  description: string
  timestamp: number
  role?: string
}

interface ActivityItemProps {
  activity: Activity
  className?: string
}

const iconMap: Record<ActivityType, React.ReactNode> = {
  status_change: <CheckCircle size={14} className="text-green-500" />,
  handoff: <ArrowRightLeft size={14} className="text-blue-500" />,
  result: <FileText size={14} className="text-purple-500" />,
  acceptance: <CheckCircle size={14} className="text-amber-500" />,
}

function formatRelativeTime(timestamp: number): string {
  const now = Date.now()
  const diff = now - timestamp
  const minutes = Math.floor(diff / 60000)
  const hours = Math.floor(diff / 3600000)

  if (minutes < 1) return 'just now'
  if (minutes < 60) return `${minutes}m ago`
  if (hours < 24) return `${hours}h ago`
  return `${Math.floor(hours / 24)}d ago`
}

export function ActivityItem({
  activity,
  className,
}: ActivityItemProps): React.ReactNode {
  return (
    <div className={cn('flex items-start gap-2 py-2', className)}>
      <span className="mt-0.5 shrink-0">{iconMap[activity.type]}</span>
      <div className="min-w-0 flex-1">
        <div className="text-xs text-foreground">
          <span className="font-medium">{activity.pairName}</span>
          <span className="text-muted-foreground"> — {activity.description}</span>
        </div>
        <div className="text-[10px] text-muted-foreground">
          {formatRelativeTime(activity.timestamp)}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
node --import tsx --test tests/recentActivity.test.ts
```

Expected: All PASS (for ActivityItem tests)

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/components/ActivityItem.tsx tests/recentActivity.test.ts
git commit -m "feat: add ActivityItem component with relative time formatting"
```

---

### Task 13: 创建 RecentActivityPanel 组件

- [ ] **Step 1: Add test for RecentActivityPanel**

Add to `tests/recentActivity.test.ts`:

```ts
const recentActivityPanel = await readFile(
  new URL('../src/renderer/src/components/RecentActivityPanel.tsx', import.meta.url),
  'utf8'
)

test('RecentActivityPanel exports RecentActivityPanel component', () => {
  assert.match(recentActivityPanel, /export (function RecentActivityPanel|const RecentActivityPanel)/)
})

test('RecentActivityPanel invokes get_recent_activities Tauri command', () => {
  assert.match(recentActivityPanel, /invoke|get_recent_activities/)
})

test('RecentActivityPanel handles empty state', () => {
  assert.match(recentActivityPanel, /empty|no.*activity|暂无/i)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
node --import tsx --test tests/recentActivity.test.ts
```

Expected: FAIL

- [ ] **Step 3: Create RecentActivityPanel component**

Create `src/renderer/src/components/RecentActivityPanel.tsx`:

```tsx
import React, { useState, useEffect } from 'react'
import { useTranslation } from 'react-i18next'
import { invoke } from '@tauri-apps/api/core'
import { Activity, ActivityItem, type ActivityType } from './ActivityItem'

interface RecentActivityPanelProps {
  className?: string
}

export function RecentActivityPanel({
  className,
}: RecentActivityPanelProps): React.ReactNode {
  const { t } = useTranslation()
  const [activities, setActivities] = useState<Activity[]>([])
  const [loading, setLoading] = useState(true)

  const loadActivities = async () => {
    try {
      const result = (await invoke('get_recent_activities', {
        limit: 10,
      })) as Array<{
        pair_id: string
        pair_name: string
        activity_type: ActivityType
        description: string
        timestamp: number
        role?: string
      }>

      setActivities(
        result.map((a) => ({
          pairId: a.pair_id,
          pairName: a.pair_name,
          type: a.activity_type,
          description: a.description,
          timestamp: a.timestamp,
          role: a.role,
        }))
      )
    } catch (error) {
      console.warn('[RecentActivityPanel] Failed to load activities:', error)
    } finally {
      setLoading(false)
    }
  }

  useEffect(() => {
    void loadActivities()
    // Poll every 10 seconds
    const interval = setInterval(() => void loadActivities(), 10000)
    return () => clearInterval(interval)
  }, [])

  if (loading) {
    return (
      <div className="flex items-center justify-center p-4 text-sm text-muted-foreground">
        Loading...
      </div>
    )
  }

  if (activities.length === 0) {
    return (
      <div className="flex flex-col items-center justify-center p-6 text-center">
        <p className="text-sm text-muted-foreground">
          {t('dashboard.activity.empty')}
        </p>
      </div>
    )
  }

  return (
    <div className="flex flex-col overflow-hidden">
      <h2 className="mb-3 text-sm font-semibold text-foreground">
        {t('dashboard.activity.title')}
      </h2>
      <div className="flex-1 space-y-0 overflow-y-auto scrollbar-thin pr-1">
        {activities.map((activity) => (
          <ActivityItem key={`${activity.pairId}-${activity.timestamp}`} activity={activity} />
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Add i18n keys**

Add to `en/translation.json`:
```json
"dashboard": {
  "activity": {
    "title": "Recent Activity",
    "empty": "Activity will appear here when Pairs start running"
  }
}
```

Add to `zh/translation.json`:
```json
"dashboard": {
  "activity": {
    "title": "最近活动",
    "empty": "Pair 开始运行后，这里会显示最近的活动"
  }
}
```

- [ ] **Step 5: Run test to verify it passes**

```bash
node --import tsx --test tests/recentActivity.test.ts
```

Expected: All PASS

- [ ] **Step 6: Commit**

```bash
git add src/renderer/src/components/RecentActivityPanel.tsx tests/recentActivity.test.ts src/renderer/src/locales/
git commit -m "feat: add RecentActivityPanel with polling and empty state"
```

---

### Task 14: 集成 RecentActivityPanel 到 Dashboard

- [ ] **Step 1: Update Dashboard.tsx**

Modify `src/renderer/src/components/Dashboard.tsx`:

Add import and integrate the panel:

```tsx
import { RecentActivityPanel } from './RecentActivityPanel'

// In the return, add the activity panel alongside PairListSection:
<div className="flex flex-1 gap-6 overflow-hidden">
  <div className="w-2/3 overflow-hidden">
    <PairListSection ... />
  </div>
  <div className="hidden w-1/3 overflow-hidden xl:block">
    <RecentActivityPanel />
  </div>
</div>
```

- [ ] **Step 2: Run typecheck and tests**

```bash
npm run typecheck:web && npm run test:js
```

Expected: All PASS

- [ ] **Step 3: Run Rust tests**

```bash
npm run test:rust
```

Expected: All PASS

- [ ] **Step 4: Commit**

```bash
git add src/renderer/src/components/Dashboard.tsx
git commit -m "feat: integrate RecentActivityPanel into Dashboard layout"
```

---

## Chunk 5: 响应式适配 + 最终优化

**Scope:** 断点调整 + 动画优化 + 全量测试

### Task 15: 响应式布局完善

- [ ] **Step 1: Verify responsive breakpoints**

Check Dashboard.tsx has proper responsive behavior:
- `< 900px`: Stats 2×2 grid, list full width, activity hidden
- `900-1279px`: Stats 4-column, list full width, activity hidden
- `≥ 1280px`: Stats 4-column, list + activity side by side (2:1)

- [ ] **Step 2: Run typecheck and lint**

```bash
npm run typecheck && npm run lint
```

Expected: No errors

- [ ] **Step 3: Run all tests**

```bash
npm run test
```

Expected: All PASS

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "feat: complete v1.5.0 dashboard redesign with responsive layout"
```

---

## Summary of Changes

### New Files Created

| File | Purpose |
|------|---------|
| `src/renderer/src/components/StatCard.tsx` | Single stat card |
| `src/renderer/src/components/StatCards.tsx` | Stat cards container |
| `src/renderer/src/components/EmptyStateGuide.tsx` | Empty state with onboarding |
| `src/renderer/src/components/PairListGroup.tsx` | Status group container |
| `src/renderer/src/components/PairListSection.tsx` | Grouped pairs list |
| `src/renderer/src/components/QuickActionsFAB.tsx` | Floating action button |
| `src/renderer/src/components/RecentActivityPanel.tsx` | Activity timeline |
| `src/renderer/src/components/ActivityItem.tsx` | Single activity entry |
| `src-tauri/src/recent_activity.rs` | Rust activity command |
| `tests/statCard.test.ts` | StatCard tests |
| `tests/statCards.test.ts` | StatCards tests |
| `tests/emptyStateGuide.test.ts` | EmptyStateGuide tests |
| `tests/pairListSection.test.ts` | PairListSection tests |
| `tests/quickActionsFAB.test.ts` | QuickActionsFAB tests |
| `tests/recentActivity.test.ts` | Activity tests |

### Modified Files

| File | Change |
|------|--------|
| `src/renderer/src/components/Dashboard.tsx` | Complete rewrite to 3-section layout |
| `src/renderer/src/App.tsx` | Remove OnboardingWizard blocking, add FAB |
| `src-tauri/src/types.rs` | Add RecentActivity types |
| `src-tauri/src/lib.rs` | Register get_recent_activities |
| `src/renderer/src/locales/en/translation.json` | Add dashboard/activity/quickActions keys |
| `src/renderer/src/locales/zh/translation.json` | Add Chinese translations |
